{
  "name": "ccs-web",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "check": "bun run --filter '*' check"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
