# Cross-compilation configuration for aarch64-unknown-linux-gnu target
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

# GitHub Packages registry configuration
[registries.github]
index = "https://github.com/Linuxdazhao/cc_auto_switch.git"
credential-provider = ["cargo:token"]